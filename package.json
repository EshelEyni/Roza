{
  "name": "@rozaeyni/common-types",
  "version": "1.0.0",
  "main": "build/src/index.js",
  "types": "build/src/index.d.ts",
  "files": [
    "build/**/*"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "del-cli ./build",
    "build": "npm run clean && tsc"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {},
  "devDependencies": {
    "del-cli": "^5.1.0",
    "typescript": "^5.5.3"
  }
}
